{
  "name": "claps-codelab-rebuild-components",
  "private": true,
  "type": "module",
  "scripts": {
    "serve:sandbox": "node tools/serve-sandbox.mjs",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "json-logic-js": "^2.0.5"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  }
}
