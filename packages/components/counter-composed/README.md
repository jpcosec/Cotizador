# counter-composed (Step 02)

Composed component contract:

- one global counter actor
- two local child counter actors
- each child displays:
  - `local`
  - `global + local`

Child actors reuse the Step 01 machine.
