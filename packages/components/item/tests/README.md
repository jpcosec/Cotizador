# Step 03 test placeholder

Planned checks:

- quantity initialization precedence (defaults -> external context -> overrides)
- schedule precedence including `hora`
- human pricing profile text in catalog mode
- deterministic total calculation from profile terms
