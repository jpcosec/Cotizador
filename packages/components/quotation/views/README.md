# View Templates

`*.html` files in this directory are view-level template stubs.

- They describe expected bindings for each view component class.
- The current live route uses the composed demo template at
  `packages/components/quotation/ui/QuotationFlowDemo.html`.

These files are intentionally kept as scaffolding for incremental runtime wiring.
