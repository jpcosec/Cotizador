# Step 01: Project Setup

**What:** Initialize Node.js project with Vitest for testing.

**Files created:**
- `package.json` — ES modules, Vitest scripts (`test`, `test:watch`)
- `vitest.config.js` — test include pattern, globals enabled

**What it affects:** Nothing existing. New files only.

**Testable:** `npm test` runs without errors (0 tests found, no failures).

**Tests:** None yet — infrastructure only.
