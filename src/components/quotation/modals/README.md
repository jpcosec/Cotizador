# Modal Templates

`*.html` files in this directory are component-level template stubs.

- They document intended Alpine contracts per modal.
- The live sandbox wiring currently mounts `packages/components/quotation/ui/QuotationFlowDemo.html`.

When full per-modal mounting is introduced, these stubs can be promoted to runtime templates.
