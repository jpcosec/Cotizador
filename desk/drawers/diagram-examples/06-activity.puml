@startuml
title Activity Diagram Example

start
:browse;
:select client;
:build basket;
if (ready to validate?) then (yes)
  :validation;
  if (save confirmed?) then (yes)
    :completed;
  else (no)
    :back to basket;
  endif
else (no)
  :keep editing basket;
endif
stop
@enduml
